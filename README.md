
  # Check localhost visibility

  This is a code bundle for Check localhost visibility. The original project is available at https://www.figma.com/design/NouQxUo2XTAeaV9OsbwIxj/Check-localhost-visibility.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  